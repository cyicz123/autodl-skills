{
  "name": "autodl-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-dist.mjs",
    "test": "npm run build && node --test tests-ts/*.test.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.0"
  }
}
