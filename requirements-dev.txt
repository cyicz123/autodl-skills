# Development dependencies for autodl-elastic-deploy

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.6.0

# HTTP mocking for tests
responses>=0.22.0

# Production dependencies (for reference)
requests>=2.25.0
